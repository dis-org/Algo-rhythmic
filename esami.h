/* ordina tutti gli elementi minori o uguali a k */
void sort_under_k(int* array, int len, int k);          // O(n^2)

/*conta i 2 in un array di interi da 1 a 5 ordinato*/
int how_many_twos(int* array, int len);                 // O(logn)
int search_last(int* array, int f, int l, int k);       // O(logn)

/* somma elementi di un array di 1 e 2 ordinato */
int one_two_sum(int* array, int len);                   // O(logn)
